% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NicheDiv-functions.R
\name{sample.down}
\alias{sample.down}
\title{Down-sample rows from a data frame}
\usage{
sample.down(
  dataframe,
  N.rows,
  prioritize.NA.poisson = TRUE,
  poisson.lambda = NULL,
  seed = 1
)
}
\arguments{
\item{dataframe}{A \code{data.frame}, tibble, or \code{sf} object to sample from.}

\item{N.rows}{A single positive integer giving the number of rows to retain.
If \code{N.rows} is greater than or equal to the number of available rows, the
original object is returned unchanged.}

\item{prioritize.NA.poisson}{Logical; if \code{TRUE} (default), rows with fewer
missing values are given higher sampling probability using Poisson-tail
weights. If \code{FALSE}, rows are sampled uniformly without replacement.}

\item{poisson.lambda}{Optional positive numeric value controlling the
steepness of the Poisson-tail weighting applied to row-wise NA counts. If
\code{NULL}, the median row-wise NA count is used.}

\item{seed}{A single numeric value used to set the random seed for
reproducible sampling.}
}
\value{
An object containing the sampled rows. The returned object has the
same class and columns as \code{dataframe} whenever possible (including \code{sf}
objects), but includes only the sampled subset of rows.
}
\description{
Randomly sample rows without replacement from a \code{data.frame}-like object. By
default, rows with fewer missing values are favored probabilistically using
Poisson-tail weights; alternatively, uniform random sampling can be used.
}
